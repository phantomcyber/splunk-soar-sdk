[lint]
# Inherit most rules from parent SDK config, but relax rules for tests
extend-ignore = [
    "S101",    # Allow assert statements in tests
    "ANN001",  # Allow missing type annotations for test fixtures
    "ANN201",  # Allow missing return type annotations for tests
]

[lint.per-file-ignores]
"tests/**/*.py" = ["S101", "ANN001", "ANN201", "PLC0415"]
